#include<iostream>
using namespace std;
main()
	{
		int a,b, number;
		cin>>a>>b;
		number = a-b;
		if(number>10)
		{
			cout<<"Number are greater from 10 "<<endl;
		}
			else if(number<10)
		{
			cout<<"Number are Less from 10 "<<endl;
		}
			if(number==10)
		{
			cout<<"Number are eequal to  10 "<<endl;
		}
	}
