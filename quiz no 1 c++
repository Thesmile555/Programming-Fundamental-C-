#include<iostream>
using namespace std;
main()
	{
   
   
     int negative;
	 int sum;
     int number;


    cout << " From where you want to --> Start ";
    	do {
			cin >> number;
			sum = sum + number;
			cout << sum << endl;
			}
   
    while(number<negative);
    
    	if(number>negative )
	{
		 cout << " " <<number;
         number ++;
    }
   
    
  	}
